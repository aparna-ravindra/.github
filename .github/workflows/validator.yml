name: Aparna CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: linux
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Run validator
      uses: aparna-ravindra/workflow-template-validator@v0.2
      with:
          path-to-workflow-yaml: './workflow-templates'
          path-to-properties-json: './workflow-templates'
          path-to-icons: './workflow-templates'
